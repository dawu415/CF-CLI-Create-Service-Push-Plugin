---
create-services:
- name:   "Credentials-UPS"
  type:   "credentials"
  credentials:
    host: "https://((environment)).mydatabase.com((endpoint))"
    username: ((username))
    password: ((password))